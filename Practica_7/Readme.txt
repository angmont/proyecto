En el servidor que se utilizó para la practica
solamente contenía un único archivo con la
extensión .msg, entonces es lo único que 
contiene en la carpeta TXT, pero si se prueba 
con otro servidor (por ejemplo, el que está con
un # en la funcion main) también puede registrar
archivos .txt
